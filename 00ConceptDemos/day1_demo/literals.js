let greet = `Welcome to ES2015`;
console.log(greet);

let name = 'JavaScript';
console.log(`${name} is awesome`);

let num1 = 20;
let num2 = 15;
console.log(`Sum of ${num1} and ${num2} is ${num1+num2}`);

// multiline
const msg1 =   `This is along msg\n` + `that spans
multiple lines\n`+`in the code`;
console.log(msg1);
